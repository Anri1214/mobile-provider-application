/**
 * Detection parameters type.
 * @typedef {Object} Detect
 * @property {String} spn
 * @property {String} imsi
 */

/**
 * Dialogbox parameters type.
 * @typedef {Object} Dialogbox
 * @property {Function} callback
 * @property {Object} event
 * @property {Object} scope
 */

/**
 * Provider parameters type.
 * @typedef {Object} Provider
 * @property {String} providerName
 * @property {String} mcc
 * @property {String} mnc
 * @property {Array} spns
 * @property {Array} imsis
 */
